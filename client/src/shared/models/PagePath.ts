export const enum PagePath {
  root ="/",
  profile = "/profile",
  myTasks = "/my-tasks",
  login = "/login",
  registration = "/registration",
  projects = "/projects",
  project = "/project/:project_id"
}